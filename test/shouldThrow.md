## should import with caps
```ts
import useCaps from 'UseCaps';
Object.assign(input, await useCaps.process(input));
```

```ts
input.shouldThrow = 'yup';
```

```ts
throw new Error('This should throw')
```
